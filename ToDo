# Must Do Now
Set /keys directory of Tor to other but "./keys"

# Priority 1
[-] Use Tcpdump files and Wireshark to analyze the network traffic
[-] Create a script to analyse timings between relays!
[-] Add a Verification Function on 'build.sh' to verify that every node has(n't) bootstrapped!
[-] Add Jitter to Tor Network

# Priority 2
[-] Optimize Dockerfile 
[-] Try to abstract Dockerfiles for better scaling capabilities
    [-] Check for abstraction on torrc files (same torrc for relays)
[-] Optimize Torrc Files:
    [-] Check Setting Pre Roles to Nodes

# Priority 3
[-] Beautify build.sh [Loading, Progress bar, Colors, Emojis, etc.]
[-] Add README.md & LICENSE to the repository

## ALWAYS!
[-] WORK MOREEEEE!
[-] BE AWESOME!
